class Solution {
public:
    bool containsDuplicate(vector<int>& num) {
        std::sort(num.begin(),num.end());
        for(int i=0;i<num.size()-1;i++){
            if(num[i] == num[i+1]){
                return true;
            }
        }
     return false;   
    }
};
